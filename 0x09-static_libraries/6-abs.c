#include "main.h"

/**
 * _abs -it will compute the absolute value of an integer
 * @n: the integer to check
 * Return: int absolute
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
